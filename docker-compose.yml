version: "3.8"

services:
  channelwepapi:
    build:
      context: ./ChannelWepApi
      dockerfile: Dockerfile
    image: channelwepapi:latest
    ports:
      - "8080:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
